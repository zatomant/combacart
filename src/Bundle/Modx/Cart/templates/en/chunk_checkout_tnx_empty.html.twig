<div class="px-4 py-5 my-2 text-center">
    <h1 class="display-5 fw-bold text-body-emphasis">Thank you!</h1>
    <div class="col-lg-6 mx-auto">
        <p class="lead mb-4">Information about the order is available in your {{ lang.cabinet }} or via the link provided by our managers.</p>
        <div class="d-grid gap-2 d-sm-flex justify-content-sm-center">
            <a href="{{ page.cabinet }}" type="button" class="btn btn-primary btn-lg px-4 gap-3">{{ lang.cabinet }}</a>
            <a href="/" type="button" class="btn btn-outline-secondary btn-lg px-4">{{ lang.action_goto_index }}</a>
        </div>
    </div>
</div>
{% if marketplace.regime %}
    <p><strong>And just in case, remember the working hours of the online order processing department:</strong></p>
    <ul>
        {% for r in marketplace.regime %}
            <li>{{ r }}</li>
        {% endfor %}
    </ul>
{% endif %}

{{ include('contact.html.twig')|raw }}
